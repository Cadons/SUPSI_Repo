package sweng.myproj.backend.view;

public class PrinterGUI extends Printer {
    @Override
    public String print(Printable printable) {
        //return super.print(printable).toUpperCase();
        return super.print(printable);//nothing to do
    }
}
